# Prompt Pulse - Frontend Dashboard

This is the Next.js application for the Prompt Pulse dashboard. It is built using the new App Router and styled with Tailwind CSS.

## Development

To run the development server locally:

```bash
npm run dev
```

The app should be running on `http://localhost:3000`.
